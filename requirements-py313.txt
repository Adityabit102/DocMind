# ════════════════════════════════════════════════════════════════════
#  DocMind — LOCAL requirements for Python 3.13 (loosened pins).
#  The pinned `requirements.txt` targets Python 3.11 (used by Docker).
#  On 3.13 several spec pins have no wheels (faiss-cpu 1.8.0, pydantic-core
#  2.7.4, etc.), so this file uses 3.13-compatible ranges instead.
#
#  Install:  pip install -r requirements-py313.txt
# ════════════════════════════════════════════════════════════════════

# ── Core RAG framework (0.3 line — keeps langchain.retrievers paths) ──
langchain>=0.3,<0.4
langchain-community>=0.3,<0.4
langchain-text-splitters>=0.3,<0.4
langchain-openai>=0.2,<0.3
langchain-anthropic>=0.2,<0.3
langchain-ollama>=0.2,<0.3
langchain-huggingface>=0.1,<0.2

# ── API ──────────────────────────────────────────────────────────────
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9
sse-starlette>=2.1
pydantic>=2.9
pydantic-settings>=2.5

# ── Vector store + retrieval ─────────────────────────────────────────
faiss-cpu>=1.9
rank-bm25>=0.2.2

# ── Embeddings + reranking (torch wheels exist for 3.13) ─────────────
sentence-transformers>=3.1
torch>=2.4

# ── LLM clients ──────────────────────────────────────────────────────
openai>=1.40
anthropic>=0.34

# ── Document processing ──────────────────────────────────────────────
pypdf>=4.2
pytesseract>=0.3.10
pdf2image>=1.17
Pillow>=10.3
python-docx>=1.1
docx2txt>=0.8
chardet>=5.2
beautifulsoup4>=4.12

# ── Cache ────────────────────────────────────────────────────────────
redis>=5.0

# ── Observability ────────────────────────────────────────────────────
prometheus-client>=0.20
python-json-logger>=2.0.7

# ── Security ─────────────────────────────────────────────────────────
slowapi>=0.1.9

# ── Utilities ────────────────────────────────────────────────────────
python-dotenv>=1.0
httpx>=0.27
tenacity>=8.4
numpy>=1.26
pandas>=2.2

# ── Optional / heavy (uncomment if you need these features) ──────────
# These are imported lazily, so the app boots and answers without them.
# ragas>=0.2                 # RAGAS evaluation page
# datasets>=2.20             # RAGAS test sets
# evidently>=0.4             # embedding drift detection
# unstructured[pdf]>=0.15    # advanced PDF/table extraction
# presidio-analyzer>=2.2     # Presidio PII redaction (else regex fallback is used)
# presidio-anonymizer>=2.2
# langchain-redis>=0.1       # Redis semantic cache (else in-memory is used)

# PDF export of conversations
reportlab>=4

# ════════════════════════════════════════════════════════════════════
#  DocMind — Model-Building Extension requirements.
#
#  These deps power the NEW, parallel model-building layer only
#  (finetune/, vlm_module/, eval_harness/). They are intentionally kept
#  OUT of the main requirements.txt / deployment image so the existing
#  RAG service and its Docker build stay exactly as they were.
#
#  Install only when you want to train / evaluate the new components:
#      pip install -r requirements-modelbuild.txt
#
#  torch + transformers already ship in the base requirements, so they
#  are re-declared loosely here rather than pinned, to avoid conflicts.
# ════════════════════════════════════════════════════════════════════

# ── Core fine-tuning stack (LoRA / PEFT) ────────────────────────────
peft>=0.11
accelerate>=0.30
datasets>=2.20

# transformers + torch are already provided by the base image; listed
# here only so a standalone install of this file still works.
transformers>=4.44
torch>=2.4

# ── Metrics + reporting ─────────────────────────────────────────────
scikit-learn>=1.3
matplotlib>=3.7
numpy>=1.26
pandas>=2.2
Pillow>=10.3

# ── OCR baseline (Python wrapper; needs the Tesseract system binary,
#    e.g. `brew install tesseract` / `apt-get install tesseract-ocr`).
#    The baseline degrades gracefully and is skipped if the binary is
#    absent, so this stays optional. ─────────────────────────────────
pytesseract>=0.3.10

# ── Optional: only needed if you fine-tune / run the VLM (Donut, etc.)
#    Donut ships within transformers; sentencepiece is its tokenizer dep.
sentencepiece>=0.2

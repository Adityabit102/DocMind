# ════════════════════════════════════════════════════════════════════
#  DocMind — deployment requirements (Docker, Python 3.11).
#  Pinned to the 0.3 langchain line that the app is actually built and
#  tested against. Heavy/optional integrations are lazily imported and
#  degrade gracefully, so they're left out of the default image (see the
#  "Optional" block to enable them).
# ════════════════════════════════════════════════════════════════════

# ── Core RAG framework (0.3 line) ───────────────────────────────────
langchain==0.3.30
langchain-community==0.3.31
langchain-text-splitters==0.3.11
langchain-openai==0.2.14
langchain-anthropic==0.2.4
langchain-ollama==0.2.3
langchain-huggingface==0.1.2

# ── API ─────────────────────────────────────────────────────────────
fastapi>=0.115,<0.139
uvicorn[standard]>=0.30
python-multipart>=0.0.9
sse-starlette>=2.1
pydantic>=2.9,<3
pydantic-settings>=2.5

# ── Vector store + retrieval ────────────────────────────────────────
faiss-cpu>=1.9
rank-bm25>=0.2.2

# ── Embeddings + reranking ──────────────────────────────────────────
sentence-transformers>=3.1
torch>=2.4

# ── LLM clients (the OpenAI client also powers Groq's compatible API) ─
openai>=1.40
anthropic>=0.34

# ── Document processing ─────────────────────────────────────────────
pypdf>=4.2
pytesseract>=0.3.10
pdf2image>=1.17
Pillow>=10.3
python-docx>=1.1
docx2txt>=0.8
chardet>=5.2
beautifulsoup4>=4.12

# ── Cache ───────────────────────────────────────────────────────────
redis>=5.0

# ── Observability ───────────────────────────────────────────────────
prometheus-client>=0.20
python-json-logger>=2.0.7

# ── Security ────────────────────────────────────────────────────────
slowapi>=0.1.9

# ── Utilities ───────────────────────────────────────────────────────
python-dotenv>=1.0
httpx>=0.27
tenacity>=8.4
numpy>=1.26
pandas>=2.2

# ── PDF export of conversations ─────────────────────────────────────
reportlab>=4

# ── Optional / heavy integrations (lazily imported; graceful fallbacks
#    are used when absent). Uncomment to enable in the image:
#      - ragas/datasets   -> RAGAS metrics (else the LLM-judge evaluator runs)
#      - evidently        -> drift detection (else a numpy fallback runs)
#      - unstructured     -> advanced PDF/table extraction (else pypdf)
#      - presidio-*       -> ML PII redaction (else a regex fallback)
#      - langchain-redis  -> Redis semantic cache (else in-memory)
# ragas>=0.2
# datasets>=2.20
# evidently>=0.4
# unstructured[pdf]>=0.15
# presidio-analyzer>=2.2
# presidio-anonymizer>=2.2
# langchain-redis>=0.1
